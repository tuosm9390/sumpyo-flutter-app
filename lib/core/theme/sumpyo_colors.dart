import 'package:flutter/material.dart';

class SumpyoColors {
  // Warm White (Background, Paper texture)
  static const Color warmWhite = Color(0xFFF9F8F6);

  // Sage Green (Primary, Buttons, Pharmacist's gown)
  static const Color sageGreen = Color(0xFFA8B5A2);

  // Mute Blue (Secondary, Music/Audit elements, Header)
  static const Color muteBlue = Color(0xFFA1B5C1);

  // Soft Charcoal (Text, Readability)
  static const Color softCharcoal = Color(0xFF4A4A4A);

  // Additional variants
  static const Color paperBorder = Color(0xFFE8E5E1);
  static const Color errorRed = Color(0xFFE57373);
}
